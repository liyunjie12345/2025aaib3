# week01-2b.py LeetCode 學習計畫 Python 版本2 使用字串的find()函式
# LeetCode 28. Find the Index of the First Occurrence in a string
class Solution:
    def strStr(self, haystack: str, needle: str) -> int:
        return haystack.find(needle) # 只有一行
